pipeline:
  build-dev:
    image: plugins/docker
    secrets: [docker_username, docker_password]
    registry: dev.enecuum.com:2087
    repo: dev.enecuum.com:2087/testnet/node-dev
    tags: latest
    when:
      branch:
        - Dev
      event:
        - push
        - tag
  notify-build:
    image: dev.enecuum.com:2087/library/drone-telegram:v1.0.0
    secrets: [telegram_token, telegram_id]
    when:
      branch:
        - Dev
      status:
        - success
        - failure
      event:
        - push
        - tag
  deploy-bootnode-dev:
    image: peloton/drone-rancher
    url: https://testnet.enecuum.com:2096
    secrets:
      - source: rancher_access_key
        target: PLUGIN_ACCESS_KEY
      - source: rancher_secret_key
        target: PLUGIN_SECRET_KEY
    docker_image: dev.enecuum.com:2087/testnet/node-dev:latest
    service: bootnode/bootnode-dev
    start_first: false
    batch_size: 1
    confirm: true
    timeout: 240
    when:
      branch:
        - Dev
      status:
        - success
      event:
        - push
        - tag
  deploy-node-dev:
    image: peloton/drone-rancher
    url: https://testnet.enecuum.com:2096
    secrets:
      - source: rancher_access_key
        target: PLUGIN_ACCESS_KEY
      - source: rancher_secret_key
        target: PLUGIN_SECRET_KEY
    docker_image: dev.enecuum.com:2087/testnet/node-dev:latest
    service: node/node-dev
    batch_size: 9
    confirm: true
    timeout: 240
    when:
      branch:
        - Dev
      status:
        - success
      event:
        - push
        - tag
  notify-deploy-dev:
    image: dev.enecuum.com:2087/library/drone-telegram:v1.0.0
    secrets: [telegram_token, telegram_id]
    message: \u2705 ${DRONE_REPO}:`${DRONE_COMMIT_BRANCH}` ${DRONE_BUILD_NUMBER} deployment successful
    when:
      branch:
        - Dev
      status:
        - success
      event:
        - push
        - tag
